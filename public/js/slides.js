jQuery(document).ready(function($){
      
    $('.myslide').slick({
    	lazyLoad: 'ondemand',
    	dots: false,
        speed: 500,
        centerMode: true,
  		centerPadding: '0px',
        // variableWidth: true,
        cssEase: 'ease',
    });

});